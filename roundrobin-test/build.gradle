apply plugin: 'org.springframework.boot'

dependencies {
    compile("junit:junit")
    compile("org.springframework.boot:spring-boot-starter-web")
    compile("com.fasterxml.jackson.datatype:jackson-datatype-joda")
    compile project(':roundrobin-core')
}
